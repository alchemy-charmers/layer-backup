includes: ['layer:basic']  # if you use any interfaces, add them here
defines:
  backup-name:
    type: string
    default: "backup"
    description: "Name of the bakcup files to create"
  backup-files:
    type: string
    default: ""
    description: "List of files to include, must be full path, can use charm conifg variables"
  backup-method:
    type: string
    default: "tgz"
    description: "The method for backup: tbz or sync"
